import PropTypes from 'prop-types'

export default {
    color: {
        type: PropTypes.string,
        def: 'rgba(0,0,0,0.5)'
    },
    duration: {
        type: PropTypes.string,
        def: '2s'
    }
}